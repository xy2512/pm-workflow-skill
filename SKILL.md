---
name: "pm-flow"
description: "Guides PMs through an 8-step agile workflow: dialogue-based requirement gathering, competitive analysis, market validation, PRD drafting, HTML prototype creation with Tailwind CSS, mermaid flowcharts, and final PRD with iframe slices. This skill should be used when the user provides an initial product idea or requirement description and needs a structured process from discovery to delivery."
---

# PM Flow — 从想法到交付的产品工作流

## 触发方式

当用户提供一个初步的产品想法、需求描述或业务场景时，严格按照以下**八个步骤**引导用户完成从需求发现到可交互交付的全流程。

**强制指令：逐步执行，每步确认后推进。绝不允许一次性输出所有步骤的结果。每个标注【等待用户确认】的步骤结束后，必须停止输出，等待用户回复。**

---

## 步骤一：需求发现与深度采集

### 1.1 接收需求

接受用户的任意形式输入（一句话、截图、语音转文字、草稿、甚至抱怨）。降低认知门槛，不做模板化要求。

### 1.2 六维评估雷达

接收需求后，**立即输出「需求成熟度雷达图」**，帮助用户直观了解当前需求的完备程度。六个维度：

| 维度 | 等级 | 说明 |
|---|---|---|
| 🎯 问题定义 | 明确/模糊/缺失 | 是否清楚解决了什么问题 |
| 👥 用户画像 | 明确/模糊/缺失 | 是否知道谁在什么场景下用 |
| 📐 核心路径 | 明确/模糊/缺失 | 用户从进入到完成的关键操作链 |
| 🏢 业务约束 | 明确/模糊/缺失 | 技术/合规/资源上的限制 |
| 📊 成功指标 | 明确/模糊/缺失 | 如何判断做成了 |
| 🔍 市场认知 | 明确/模糊/缺失 | 竞品格局和差异化方向 |

每个维度标注等级，并用简短一句话解释判断依据。对于「模糊」和「缺失」的维度，进入 1.3 进行针对性追问。

### 1.3 智能追问（自适应轮次）【等待用户确认】

**追问策略：基于雷达图的薄弱维度进行定向深挖，而非无差别轰炸。**

- **第 1 轮**：聚焦雷达图中标记为「缺失」的维度，每个维度提 1 个开放式问题。
- **第 2 轮**：基于用户回答，深入挖掘「模糊」维度的细节，并引出用户未提及但产品必需的隐性需求（如：权限模型、数据流转、异常兜底）。
- **第 3 轮（如需要）**：针对前两轮中用户回答自相矛盾或逻辑跳跃的地方，温和确认并补全。
- **提前终止条件**：如果雷达图 6 个维度全部达到「明确」等级，可跳过后续追问轮次，直接进入总结确认。

**追问原则：**
- 采用开放式、情境化提问（如「你能描述一个用户最常遇到的失败场景吗？」），避免死板的选项列表
- 每次提问 2-4 个，不超过 4 个，避免信息过载
- 基于用户的行业和需求规模自适应调整追问深度（一个小工具 vs 一个企业级系统，追问深度应不同）

### 1.4 结构化确认

当追问完成或用户明确表示「可以开始了」，输出一份**需求摘要卡片**：

```
┌─────────────────────────────────────┐
│ 需求摘要                             │
├─────────────────────────────────────┤
│ 问题：    [一句话定义核心问题]         │
│ 用户：    [目标用户和核心场景]         │
│ 方案：    [初步解决思路]              │
│ 约束：    [关键限制条件]              │
│ 指标：    [预期成功指标]              │
│ 差异化：  [相比现有方案的独特价值]     │
└─────────────────────────────────────┘
```

请用户确认：**「这份摘要是否准确反映了你的意图？有需要补充或修正的吗？」**

**【等待用户确认】** — 用户确认后进入步骤二。

---

## 步骤二：竞品分析与市场定位

在动手设计前，先建立外部视野，避免闭门造车。

### 2.1 竞品全景图

基于步骤一中收集到的「参考/竞品」信息，以及 AI 自身的知识库，输出一份**竞品分析简报**：

| 对比维度 | 我方产品 | 竞品 A | 竞品 B |
|---|---|---|---|
| 核心定位 | | | |
| 目标用户 | | | |
| 核心功能 | | | |
| 用户体验亮点 | | | |
| 定价/商业模式 | | | |
| 明显短板 | | | |

**分析要求：**
- 至少分析 2-3 个直接或间接竞品
- 不仅要列出功能，更要分析**用户选择它的原因**和**用户离开它的原因**
- 基于分析结果，明确我方产品的**差异化切入点**（功能差异、体验差异、场景差异、价格差异，至少明确 1 个）

### 2.2 市场验证建议

根据产品类型，给出 1-3 条可执行的验证建议（非必须执行，但建议用户考虑）：

- **定性验证**：用户访谈（5-8 人）、竞品用户评论分析
- **定量验证**：问卷调查、搜索趋势分析、已有数据挖掘
- **低成本实验**：Landing Page 投放测试、社群发帖测反馈、MVP 功能裁剪方案

### 2.3 用户确认

向用户展示竞品分析简报和市场验证建议，询问：
**「基于竞品分析，您觉得我们的差异化方向是否准确？是否需要调整产品定位？」**

**【等待用户确认】** — 用户确认后进入步骤三。

---

## 步骤三：项目初始化与目录搭建

在所有前期分析确认后，建立规范的项目文件结构。

### 3.1 标准目录树

为当前项目创建专属根文件夹，内含：

```
{项目名称}/
├── prd/              ← 产品需求文档 (HTML 格式)
├── prototype/        ← 高保真 HTML 原型
├── flowcharts/       ← Mermaid 流程图 (Markdown/图片)
├── research/         ← 竞品分析、用户调研等前期研究资料
└── annex/            ← 数据字典、接口文档等附件
```

### 3.2 确认架构

向用户展示目录结构，并说明每个目录的用途和后续产出物的存放规则。

---

## 步骤四：产出初版 PRD

### 4.1 产出要求

基于步骤一的需求摘要和步骤二的竞品分析，生成一份**详细初版 HTML 格式 PRD**（存放在 `prd/` 目录下）。

必须包含的章节（参考 `references/prd_structure.md` 中的完整规范）：
1. **项目信息** — 产品名称、版本、负责人、核心目标
2. **需求背景与目标** — 现状问题、紧迫性、目标矩阵（目标类型/描述/指标/目标值）
3. **用户画像与旅程图** — 典型用户画像、User Journey Map（含阶段/触点/行为/痛点/机会点）
4. **市场定位摘要** — 差异化切入点一句话概括（来自步骤二的结论）
5. **功能清单与优先级** — 按 P0/P1/P2 分级，每个功能点需有简要描述
6. **核心业务逻辑** — 数据流向、状态机、关键业务规则

*以下章节标注「待原型确认后补充」：*
- 详细方案（带原型切片）
- 业务流程图（Mermaid）
- 异常与边界处理
- 埋点方案

### 4.2 HTML 设计规范

PRD 页面必须满足以下设计要求（参考 `references/html_design_spec.md`）：
- 使用 **Tailwind CSS CDN**，确保独立运行
- 配色方案：主色 `#2563EB`（蓝）、背景 `#F8FAFC`、文字 `#1E293B`、辅助灰 `#94A3B8`
- 左侧固定目录导航（TOC），支持滚动高亮当前章节
- 响应式布局，在不同屏幕宽度下保持可读性
- 表格、代码块、引用块均有清晰的视觉层级
- 页面顶部包含项目名称、版本号和版本切换下拉菜单预留位

### 4.3 用户确认

**「这是初版 PRD 的骨架和核心内容，方向和基础逻辑准确吗？确认后我们将进入原型设计环节。」**

**【等待用户确认】** — 用户确认后进入步骤五。

---

## 步骤五：高保真 HTML 原型

### 5.1 原型技术规范

1. **单文件 HTML**，包含完整内联 CSS 和 JS，确保独立运行
2. 使用 **Tailwind CSS CDN**，采用现代简洁设计风格
3. 必须实现关键交互状态：默认态、hover/focus 态、加载态、空状态、错误态
4. 页面切换通过 **URL Hash 路由**（如 `#login`、`#dashboard`）实现
5. **Focus 模式**：解析 URL 参数 `?focus=feature_id`，自动锁定非目标功能区域（`pointer-events: none; opacity: 0.5; filter: blur(2px)`），仅允许操作目标功能点

### 5.2 设计质量要求

- 配色与 PRD 页面保持一致（同色系）
- 移动端原型： viewport `width=device-width`，最大宽度 430px
- Web 端原型：响应式布局，推荐最小宽度 1280px
- 所有交互元素必须有明确的视觉反馈（hover 变色、点击缩放、loading 动画）
- 空状态页面需包含引导性插画占位和操作提示文案

### 5.3 原型审查与迭代【等待用户反馈】

- 使用 `preview_url` 工具呈现可交互的原型预览
- 引导用户从真实用户视角审视：「用户到这里会犹豫吗？」「返回按钮在哪？」「断了网怎么办？」
- **PRD 实时同步**：每次修改原型后，同步更新 PRD 中「详细方案」部分的逻辑描述，确保文档与原型版本一致

**【等待用户反馈】** — 用户满意后进入步骤六。

---

## 步骤六：流程图与架构图

在原型验证完成后，业务逻辑已清晰，此时产出可视化流程图。

### 6.1 流程图要求

- 使用 **Mermaid** 语法
- 必须包含：
  1. **核心操作流程图**（`flowchart TD`）— 用户从进入到完成目标的主链路
  2. **异常分支流程** — 网络错误、权限不足、数据为空等边界情况
  3. **关键时序图**（`sequenceDiagram`）— 涉及多方交互的核心场景（如：用户-系统-第三方）

### 6.2 注意事项

- 避免使用未转义的特殊字符（特别是中文括号 `（）` 须转义为 `\（\）`）
- 流程图存放在 `flowcharts/` 目录下，以 Markdown 文件格式保存
- 将流程图以 Mermaid 代码块的形式嵌入最终 PRD 对应章节

---

## 步骤七：最终版 PRD

综合所有成果，产出完整的最终版 PRD。

### 7.1 最终版 PRD 结构

遵循 `references/prd_structure.md` 中的完整章节规范：

1. 项目信息与版本记录
2. 需求背景与目标
3. 用户画像与使用场景
4. 市场定位与竞品摘要
5. 需求功能清单（P0/P1/P2）
6. **详细方案**（核心章节，见 7.2）
7. 业务流程图（Mermaid 嵌入）
8. 异常与边界处理
9. 数据追踪与埋点
10. 未来演进规划 (Roadmap)
11. 附件

### 7.2 详细方案：模块化展示

每个核心功能点采用**三栏布局**：

```
┌──────────────────────────────────────────────┐
│ 功能模块：用户登录                              │
├──────────────────────────────────────────────┤
│ ┌──────────────┐ ┌──────────┐ ┌───────────┐ │
│ │ 交互流程图    │ │ 规则描述  │ │ 原型沙盒   │ │
│ │ (Mermaid)    │ │ (文字)    │ │ (iframe)  │ │
│ └──────────────┘ └──────────┘ └───────────┘ │
└──────────────────────────────────────────────┘
```

**iframe 切片规范：**
- Hash 定位：`src="../prototype/prototype_v1.0.html#login"`
- Focus 锁定：`?focus=login` 参数控制只展示目标功能
- 移动端尺寸：`width:375px; height:812px; border:none;`
- Web 端尺寸：`width:100%; height:600px; border:none;`
- 沙盒隔离：`sandbox="allow-scripts allow-same-origin"`

**HTML 示例**：

```html
<div class="feature-module">
  <h3>功能：用户登录</h3>
  <div class="feature-content" style="display: flex; gap: 20px;">
    <div class="logic-rules" style="flex: 1;">
      <h4>交互流程图</h4>
      <div class="mermaid">flowchart TD ...</div>
      <h4>规则描述</h4>
      <ul>
        <li><strong>触发条件</strong>: ...</li>
        <li><strong>交互反馈</strong>: ...</li>
        <li><strong>异常处理</strong>: ...</li>
      </ul>
    </div>
    <div class="sandbox-preview">
      <iframe src="../prototype/prototype_v1.0.html?focus=login#login"
              style="width:375px; height:812px; border:none;"
              sandbox="allow-scripts allow-same-origin">
      </iframe>
    </div>
  </div>
</div>
```

### 7.3 交付检查清单

- [ ] 包含清晰的用户旅程图（User Journey Map）
- [ ] 每个功能点都有对应的 Mermaid 交互流程图
- [ ] 每个原型沙盒都启用了 Focus 模式锁定
- [ ] 异常场景（断网、空数据、权限不足、并发冲突）已补充
- [ ] 市场定位和差异化策略已纳入文档
- [ ] 竞品分析结论与功能设计有明确的对应关系
- [ ] 版本记录完整，记录了从 v0.5 到最终版的所有变更

---

## 步骤八：版本迭代管理

### 8.1 物理隔离策略

- **绝对不要覆盖历史版本**
- 新版本复制旧版本文件并重命名（如 `prd_v1.0.html` → `prd_v1.1.html`）
- PRD 和 prototype 的版本号保持严格对应

### 8.2 版本切换器

- PRD 页面右上角提供版本切换下拉菜单
- 查看历史版本时显示醒目提示横幅：「您正在查看历史版本 vX.X，点击此处前往最新版」

### 8.3 变更日志与联动

- 新版 PRD 的版本记录表中详细列出：新增功能、修改功能、下线功能
- 所有 iframe 切片路径自动更新为对应版本的原型文件

---

## 参考资源

- `references/prd_structure.md` — PRD 完整章节规范和模板
- `references/html_design_spec.md` — HTML 页面设计规范（配色、字体、布局、组件）
- `references/competitive_analysis_template.md` — 竞品分析模板
- `assets/prd_template.html` — HTML 格式 PRD 基础模板（可直接复制使用）
